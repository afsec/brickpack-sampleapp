version: "3"
services:
  concierge-db:
    image: "afsec/concierge-db:latest"
    # volumes:
      # - concierge_data:/data
    restart: always
    environment:
      # DB_CLIENT_TOKEN: 9admin9
      # HTTP_CLIENT_TOKEN: 9admin9
      CLIENT_TOKEN: 9admin9


  brickpack-sampleapp:
    depends_on:
      - concierge-db
    environment:
      # SERVER_TOKEN: 9admin9
      # DB_CLIENT_TOKEN: 9admin9
      # HTTP_CLIENT_TOKEN: 9admin9
      BP_SERVER_TOKEN: 9admin9

    build: .
    ports:
      - "8000:8000"

# volumes:
  # concierge_data: {}
